# --- Servidor ---
PORT=3001
NODE_ENV=development
BASE_URL=http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000

# --- Anthropic ---
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# --- Gmail OAuth ---
GMAIL_CLIENT_ID=xxxx.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=GOCSPX-...
GMAIL_REFRESH_TOKEN=1//...
GMAIL_FROM_EMAIL=karia@tudominio.com

# --- Auth ---
KARIA_API_KEY=tu-api-key-secreta
SECRET_KEY=clave-secreta-para-hmac-cambiar-en-produccion
JWT_SECRET=clave-secreta-para-jwt-cambiar-en-produccion
JWT_EXPIRATION_HOURS=8

# --- Cifrado (generar con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())") ---
ENCRYPTION_KEY=tu-fernet-key-base64

# --- Rate Limiting ---
RATE_LIMIT_GENERAL=120/minute
RATE_LIMIT_COMPOSE=10/minute
RATE_LIMIT_SEND=5/minute

# --- PostgreSQL local / AWS RDS ---
PG_HOST=localhost
PG_PORT=5432
PG_USER=karia_reach_user
PG_PASSWORD=your-local-postgres-password
PG_DATABASE=karia_reach
PG_POOL_MIN_SIZE=2
PG_POOL_MAX_SIZE=10

# --- Gmail OAuth por usuario ---
GMAIL_OAUTH_REDIRECT_URI=http://localhost:3001/api/gmail/oauth/callback
GMAIL_SUPERADMIN_FALLBACK=true
